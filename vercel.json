{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "functions": {
        "api/getImageMeta.js": { "runtime": "edge" },
        "api/getImageUrl.js": { "runtime": "edge" }
    }
}
